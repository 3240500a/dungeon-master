// Пассивы прокачиваются в визуальном граф-вью (treeView.ts). Этот файл оставлен
// тонким ре-экспортом для обратной совместимости имён.
export { renderPassiveTree as renderPassiveSection } from './treeView.js';
