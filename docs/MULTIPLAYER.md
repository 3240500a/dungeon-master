# MULTIPLAYER.md — дорожная карта кооператива

**Цель:** несколько игроков в одном забеге. Опирается на уже готовое авторитетное ядро
`GameSession` (см. [GAMESESSION.md](GAMESESSION.md)). Это **план**, не реализация.

Модель: **комната = один `GameSession` на сервере**. Клиенты шлют ввод (`PlayerInput`),
получают снапшоты `WorldState`. Тот же код ядра, что гоняет сим-бота и рисует клиент.

---

## Что уже готово (фундамент)
- **Авторитетное ядро** `shared/session/GameSession` — headless, детерминированное
  (сид-RNG, доказано `runner.test`/`sim`), сериализуемое (`WorldState` — чистые данные).
- **Мультиигрок по API:** `addPlayer(id, save)`, `players: Record<id, PlayerEntity>`,
  `tick(dt, inputs: Record<id, PlayerInput>)` — уже принимает ввод нескольких игроков.
- **Ввод сериализуем:** `PlayerInput { move, facing, attack, cast, interact }`.
- **Клиент — вью:** движение/бой/реген игрока уже идут через сессию и в городе, и в
  подземелье (`SessionController` = драйвер; игрок — чистый вид). Локальный игрок уже
  авторитетен.
- **Сервер:** есть Express + `node:sqlite`, REST `/api/auth|save|load`, zod-валидация
  сейвов (база анти-чита). charId независим — сейвы по игрокам уже разведены.

---

## Что осталось (фазы MP)

### MP-1. Rewards в авторитетное ядро
Сейчас лут/XP/золото/квесты считает КЛИЕНТ по шине (`GameSession(rewards:false)` → событие
`monster-died` разбирают LootController/Progression/quests). Для MP это рассинхронит игроков.
- Перенести начисление наград в `tick` (режим `rewards:true` уже есть — killMonster даёт
  золото/дроп/XP) или на хост; клиент только отражает.
- Дроп на землю — уже сущность `WorldState.drops`; подбор сделать через `input.interact`
  (в ядре есть `tryPickup`), а не клиентским LootController.
- Квесты слушают события сессии на хосте.

### MP-2. Транспорт (WebSocket) — ✅ СДЕЛАНО (июль 2026, always-online)
Реализовано полностью серверно-авторитетно (вся игра онлайн, оффлайна нет):
- **Сервер** (`server/src/net/`): `wsServer` (`ws` на `/ws`, общий http), `Room` = один
  `GameSession(rewards:true)` + луп 30 Гц; город/данж через `enterFloor`; команды города
  (`townActions`), голосование за спуск (все «за» → след. этаж), награды/сток магазина авторитетно;
  `roomManager` (join по коду / соло-авто).
- **Протокол** (`shared/session/netTypes.ts`): `ClientFrame`(join/input/cmd/descend/vote/leave) ↔
  `ServerFrame`(joined/snapshot/events/saveUpdate/shop/peer*/areaChanged/vote*); `serialize.ts`
  (`serializeWorld`/`floorInit`). Снапшот — только рантайм-поля по id (без тяжёлого save).
- **Клиент** (`net/netClient`,`net/netDriver`, `scenes/OnlineScene`): вью+ввод; лобби (Соло/Хост/
  Войти), модалка голосования; экономика — только `sendCmd`. Убраны оффлайн-старт, `SaveController`,
  клиентский `GameSession`/`SessionController`/`LootController`, старые Town/Dungeon/GameScene.
- Сейчас — полный снапшот 30 Гц (дельты и предсказание — MP-3). Прод: сервер отдаёт статику / `VITE_WS_URL`.

**Хвосты MP-2 (добито, июль 2026):**
- **Квесты на сервер:** shared `economy/questLogic.ts` (чистая логика), трекинг в `Room` по
  событиям сессии (`monster-died.by`/`item-picked`/`enterDungeon`), команды `acceptQuest`/
  `turnInQuest`, кадр `questBoard`, событие `quest`. Клиентские `QuestController`/`progression.ts`
  удалены; панель квестов — чистый вью. XP-прокачка — единая `economy/progression.ts gainXp`.
- **Подбор лута:** событие `item-picked` (+`saveUpdate`), клик = точечная команда `pickup{dropId}`
  (без гонки сэмплирования), E-ключ = проксимити. Кладётся в авторитетную сетку.
- **Сброс на землю:** команда `drop{uid}` → `GameSession.dropToGround` (роняет у ног).
- **Возврат в город:** портал у входа → кадр `return` → голосование (kind `town`) → `enterTown`.
- **Выбор сложности:** портал открывает `difficultyPanel` → `descend{difficultyId}`; сервер
  валидирует разблокировку, применяет к генерации/наградам, копит `difficultyProgress`.
- **Взгляд:** «нос»-указатель у пиров и монстров. Личный боевой лог/статистика восстановлены.
- Мёртвые шинные события убраны (`quest:*`, `floor:entered`, `town:entered`, `run:enter`,
  `item:drop-to-ground`).
- **Сейв — серверно-авторитетный (закрыта дыра анти-чита):** раньше клиент слал на join ВЕСЬ
  свой localStorage-сейв, и сервер брал его как истину (можно было нафабриковать золото/статы/
  предметы). Сначала перевели join на `{charId, create}` (сервер грузит/строит по charId), затем —
  на полноценные аккаунты (ниже). Присланные клиентом статы игнорируются. Стартовый набор — единый
  shared `newCharacterSave` (клиентский `startingItems` убран).

### Аккаунты (логин+пароль, владение персонажами) — ✅ СДЕЛАНО (июль 2026)
- **Хранилище** (`server/src/db/db.ts`, `node:sqlite`): `users` (ник UNIQUE NOCASE + scrypt-хеш+соль),
  `sessions` (токен→userId, TTL 30д), `characters` (charId→userId+сейв, индекс по userId). Старая
  `saves` осиротела (для сброса — удалить `data/dm.db`).
- **Пароли** (`server/src/auth/password.ts`): `node:crypto` scrypt+соль, `timingSafeEqual` — без
  сторонних зависимостей.
- **HTTP** (`server/src/index.ts`): `POST /api/register|login|logout`; `GET/POST/DELETE /api/characters`
  (authed по `Authorization: Bearer`, лимит 5, create → `newCharacterSave`). `/api/health` остался,
  мёртвые REST save/load/auth и клиентский `api.ts` удалены.
- **WS-владение** (`roomManager`): join = `{token, charId}` → `getSession`→userId, `getCharacter`
  проверяет `userId` (чужой charId → `forbidden`), сейв санитайзится и грузится; `Room` персистит
  `putCharacter(charId, userId)`.
- **Клиент** (`modules/auth/`): `authApi` + сессия в `App.auth` (`dm:auth`); `LoginScene`; ростер из
  `GET /api/characters` (не localStorage); `ClassSelect` → POST create; `OnlineScene` join `{token,charId}`,
  `app.state` из `joined`. localStorage-ростер и `saveToLocal` в панелях убраны (сейв персистит сервер);
  бинды действий переведены на команду `bind`.
- **На будущее:** email-верификация, rate-limit брутфорса, refresh-токены; прод — обязательно https/wss.

### Лобби и реконнект по намерению — ✅ СДЕЛАНО (июль 2026)
Раньше на входе сервер БЕЗУСЛОВНО реконнектил персонажа в его «грейс-комнату», игнорируя `roomCode`.
Грейс-комната возникала после ЛЮБОГО выхода (даже соло) и жила `reconnectGraceSec`=1 час → вход по коду
к другу молча перекидывал в свою старую комнату («кидает в разные комнаты»). Переделали на явное намерение:
- **Грейс-реконнект — только из подземелья.** `Room.removePlayer`: если `area==='dungeon'` — запоминаем
  игрока (`disconnected`+`onGrace`) и ждём; в **городе** выход = чистый разрыв, пустая городская комната
  сразу уничтожается (`destroyIfEmpty`). Реконнектить в городе нечего.
- **Протокол** (`netTypes.ts`): `join` получил `fresh?`/`resume?`; добавлены `runStatus`(клиент↔сервер)
  и `abandon`→`abandoned`. `roomManager`: `runStatus` сообщает, есть ли забег (комнату не трогает);
  `join{resume}` → в грейс-комнату (иначе `error 'no-run'`); `join{roomCode}`/`{fresh}` → к другу/новая
  комната; висящий забег при таком осознанном входе = бросок (штраф смерти) как страховка (порядок:
  штраф в БД → чтение урезанного сейва). `abandon` = `Room.abandonAsDead` (штраф + чистка грейса).
- **Клиент** (`OnlineScene`): на входе — плашка «Подключение…», запрос `runStatus`. Есть забег → модалка
  **«Продолжить / Забросить прохождение»** (Забросить = гибель со штрафом → обычное лобби); нет забега →
  сразу лобби (Соло/Хост/Войти шлют `fresh`/`roomCode` по УЖЕ открытому сокету). Обработчики сцены
  снимаются на пере-вход (`NetClient.off`/`clearLifecycle`) — без накопления дублей.

### MP-3. Предсказание и интерполяция — ✅ ЧАСТИЧНО (июль 2026)
Сделано (клиент-только, авторитет не меняется):
- **Интерполяция чужих сущностей** (пиры/монстры/снаряды): `net/snapshotBuffer.ts` — буфер снапшотов
  с временем приёма; рисуем их с задержкой `INTERP_DELAY_MS`=100 (в прошлом), лерп позиции + угла
  (по кратчайшей дуге) между двумя окружающими снапшотами. Убирает 30 Гц-«ступеньки» и джиттер.
  Нефизические поля (hp/alive/дебаффы) — из целевого снапшота. Тест `snapshotBuffer.test.ts`.
- **Свой игрок — сглаживание** к последней АВТОРИТЕТНОЙ позиции (экспоненциально, `SELF_SMOOTH_TAU_MS`
  =45), мгновенный снап при рассинхроне > `SELF_SNAP_DIST` (респавн/смена этажа/рывок). Не
  интерполируем себя (иначе своё движение с лагом). `NetDriver.resetInterpolation()` — на смене области.
- **Остаётся (будущее): полное предсказание ввода + реконсиляция.** Мгновенное движение своего игрока
  «в опережение» требует серверного ack последнего обработанного `input.seq` в снапшоте (переигрывать
  необработанные инпуты от подтверждённого состояния). Пока НЕ делаем: в мили-игре это даёт rubber-band
  об монстров/стены, которых клиент не симулирует (сепарация/коллизии на сервере). Дальнобойным лагам
  (>100мс интернет) поможет; для игры «со знакомым рядом» доминирует 30 Гц-ступенька, её и убрали.

### Транспорт: задержка/пинг — ✅ (июль 2026)
Тюнинг латентности WS (не меняет авторитет; особенно помогает на узком/VPN-канале):
- **`TCP_NODELAY`** ([index.ts](../packages/server/src/index.ts) `server.on('connection', s => s.setNoDelay(true))`):
  выключен алгоритм Нейгла — мелкие реалтайм-кадры (ввод/снапшоты) не ждут склейки до ~40мс.
- **Сжатие WS** ([wsServer.ts](../packages/server/src/net/wsServer.ts) `perMessageDeflate {threshold:1024}`):
  крупные снапшоты 30 Гц жмутся (мелочь <1КБ — нет), снимает забитость полосы (bufferbloat-лаг) на VPN.
- **Индикатор пинга + keepalive**: кадры `ping`/`pong` (id-эхо), клиент раз в 1с меряет RTT
  (`NetClient.rtt`) и рисует его в левом-верхнем углу (`OnlineScene.updatePing`, цвет по порогам).
  Заодно keepalive — не даёт прокси уронить простаивающее соединение.
- Про VPN: основной лаг — это крюк через exit-node (география + возможный TCP-over-TCP). Лечится на
  стороне игрока: VPN-локация рядом с сервером, WireGuard/UDP вместо OpenVPN-TCP, либо без VPN.

### MP-4. Серверная генерация этажа
- Сейчас `FloorLayout` (грид/спавн/монстры) готовит клиент (`generateDungeon` + `spawnPacks`).
  Хост генерит по общему сиду (сид уже в `WorldState.seed`) и раздаёт layout при входе на этаж.
- Синхронный переход на следующий этаж (лестница) для всей комнаты.

### MP-5. Рендер удалённых игроков
- `players` уже мульти; на клиенте рисовать остальных как виды (по аналогии с `Monster`),
  спрайт по классу; свой — предсказанный, чужие — интерполированные.

### MP-6. Лобби / коннект / реконнект
- Создать/присоединиться к комнате (код/список), выбор персонажа (charId независим).
- Сейв на выход/периодически (REST `/api/save` уже есть); при дисконнекте — грейс-период,
  затем выгрузка игрока из комнаты.
- Анти-чит: ввод валидируется симуляцией (клиент не шлёт урон/позиции), сервер — источник истины.

---

## Порядок и оценка
1. **MP-1 (rewards в ядро)** — средне; развязывает клиент от логики наград (нужно и для соло-чистоты).
2. **MP-2 (WS-транспорт + комната)** — крупно; ядро уже принимает мульти-ввод.
3. **MP-5 (рендер ремоутов)** — мелко после MP-2.
4. **MP-3 (предсказание/интерполяция)** — средне; без него будет «резина» на пинге.
5. **MP-4 (серверная floor-gen)** — средне.
6. **MP-6 (лобби/реконнект)** — средне.

Пререквизит «локальный игрок авторитетен» — **уже выполнен** (перенос движения в сессию).
Ближайший осмысленный первый шаг — **MP-1** (rewards в ядро), т.к. он полезен и вне сети.
