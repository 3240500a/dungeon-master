# Монстры: система атрибутов + экипировки + ростер (план)

Унификация: **монстр = атрибуты (STR/DEX/INT/VIT) + экипировка**, статы деривятся тем же матом, что
у игрока. «Афиксы монстра» = афиксы на его сгенерированном гире. Дополняет `docs/MONSTER_AI_PLAN.md`
(поведение по фракциям — уже сделано).

## 1. Принцип

- **Авторинг монстра:** `{ id, name, faction, subfaction?, role, tier, attrs:{str,dex,int,vit},
  weapon, armor?, offhand?, ai?, vision/visionAngle/hearing, moveSpeed?, sprite }`. Боевые статы НЕ
  задаются вручную — деривятся.
- **Рантайм (`ScaledMonster`)** — прежняя плоская форма (hp/minDamage/maxDamage/armor/acc/evade/crit/
  resists/attackSpeed/damageType/level/xp/…). Движок читает её без изменений; меняется только генератор.
- **Деривация (переиспользует `deriveStats` игрока):**
  - **VIT** → HP (+реген), **STR** → мили-урон + армор + вес, **DEX** → меткость/уворот/скор.атаки/крит
    + дальний-урон, **INT** → маг-урон + скор.каста.
  - **Урон = урон оружия × скейл по атрибуту** (STR/DEX/INT-доли по весу оружия — тот же
    `weaponAttrScaling`), **защита = из брони**, **AI и тип урона — из оружия** (мили/дальний/кастер).
  - **Уровень** растит атрибуты per-level → всё деривнутое масштабируется. Глубина = уровень монстра.

## 2. Монстро-гир (отдельная библиотека по фракциям)

Новый конфиг **`monster-gear`** — те же параметры, что у `items.base` (оружие: урон/вес/тип урона/хват/
attackType; броня: класс/защита/требования), но отдельный пул, тегированный `faction` (+ подфракция).
- Нежить — кованое/костяное (мечи/топоры/булавы/луки/арбалеты/посохи + Стёганая/Кожаная/Кольчуга/
  Сегментная/Латная + щиты).
- Звери — природное (когти/клыки/плевок как «оружие» + шкура-«броня»); конструкты — встроенное
  (кулаки/молоты/лучи); джинны — магия (ятаганы/посохи/болты). (пулы зверей/конструктов/джиннов — позже.)

Монстр ссылается на гир по id: `weapon` (обяз.), `armor?`, `offhand?` (щит). Метательный топор = `axe`
с `attackType: ranged`.

## 3. Афиксы монстров = афиксы на гире (унификация с item-движком)

Вместо `monster-affixes.json` (стат-мульты, депрекейт) — генератор **одевает монстра гиром через тот
же `rollAffixes`**:
- редкость монстра → редкость гира: normal (0 афиксов) · magic (1–2) · champion/rare (3+);
- **подфракция задаёт тему афиксов** через `tagWeights` (культ огня → огненные чаще — механизм уже есть);
- стат монстра = деривация(**атрибуты + гир с афиксами**). Гир может дропаться (по желанию).

## 4. Подфракции (визуал + тема афиксов)

Каждая фракция → подфракции: чисто визуальный скин + тема афиксов для magic/rare. Забег = одна
подфракция. Для нежити: Культ огня (fire) · Культ стужи (cold) · Культ тлена (яд/некро) · Культ грозы
(lightning) · базовый (физ/некро). Конфиг подфракций (визуал/тема) — редактируемый.

## 5. Ростер андедов (13 типов)

| # | Тип | Тир | Атрибуты | Броня | Оружие | AI |
|---|---|---|---|---|---|---|
| 1 | Скаут | слабый | DEX | без / Стёганая | кинжал (dagger) | мили, быстрый |
| 2 | Разведчик | слабый | STR·DEX | Кожаная | 1H топор/меч | мили |
| 3 | Метатель | слабый | STR·DEX | Кожаная | метат. топор (axe/ranged) | дальний |
| 4 | Боец | средний | STR·VIT | Кольчуга | 1H меч + щит | мили |
| 5 | Лучник | средний | DEX | Кожаная | лук | дальний |
| 6 | Охранник | средний | VIT·STR | Сегментная + щит | 1H булава | мили-танк |
| 7 | Колдун | средний | INT | Стёганая (роба) | посох (слабый) | кастер |
| 8 | Берсерк | средний | STR·DEX | Кольчуга | 2H секира | мили, агро |
| 9 | Рыцарь | сильный | STR·VIT | Латная | 1H меч + щит | мили-танк |
| 10 | Фанатик | сильный | STR·VIT | Латная | 2H меч | мили, агро+урон |
| 11 | Шаман | сильный | INT·VIT | Стёганая (роба) | посох (сильный) | кастер |
| 12 | Арбалетчик | сильный | STR·DEX | Сегментная | тяж. арбалет | дальний, бронир. |
| 13 | Главарь | босс | все·VIT | Латная + щит | 2H / 1H+щит | мили + аура* |

Слабый 3 · средний 5 · сильный 4 · босс 1. Пачки **70–90% мили по весам спавна** (кол-во типов ≠ ratio).
Классы брони: Стёганая<Кожаная<Кольчуга<Сегментная<Латная. (* аура/heal — блоки D/E, позже.)

## 6. Фазы
- **Ф1 — Схемы:** новый `monstersSchema` (атрибуты + гир + role/tier/subfaction + перцепция); конфиг
  `monster-gear` (faction + оружие/броня как `items.base`); конфиг `subfactions`. `ScaledMonster` рантайм — как есть.
- **Ф2 — Деривация:** `deriveMonsterStats(def, gear, level, scaling)` → `ScaledMonster` (переиспользует
  `deriveStats` + урон оружия×атрибут + защита брони; AI/тип урона из оружия).
- **Ф3 — Генератор:** `generateMonster` резолвит гир, деривит на уровне, катает гир-афиксы по редкости +
  тема подфракции, применяет к статам. `monster-affixes` депрекейт.
- **Ф4 — Контент:** андед-`monster-gear` (оружие/броня, костяное/ржавое) + 13 типов (атрибуты/роль/тир/гир) +
  подфракции; веса пачек 70–90% мили.
- **Ф5 — Редактор:** ВСЁ по монстрам настраиваемое — атрибуты/гир/роль/тир/подфракция, вкладки
  `monster-gear` и `subfactions`, **live-превью деривнутых статов** (как генератор предметов).
- **Ф6 — Тесты + verify + доки** (деривация, скейл по уровню, гир-афиксы по редкости/подфракции, ростер).
- **(позже)** пулы гира зверей/конструктов/джиннов; шаман-heal/buff; главарь-аура (блоки D/E).

## 7. Прогресс (обновляется)
- ✅ **Ф1 гир-схема + Ф4 андед-гир:** `monster-gear` (17 предметов нежити: оружие/броня/щит), схема
  дискр.-юнион по `kind`, вкладка в редакторе. (коммит `0d847dd`)
- ✅ **Ф2 деривация:** `formulas/monsterDerive.ts` — чистая `deriveMonsterStats(tpl, weapon, armor, shield,
  level)` → `MonsterDef` (VIT→hp, STR→урон/армор, DEX→меткость/уворот/скор/крит, INT→маг-урон; урон
  оружия×атрибут по весу/типу; AI/тип урона из оружия; резисты по фракции; рост атрибутов от уровня). +11 тестов.
- ✅ **Ф3 генератор + Ф4 ростер:** `generateMonster(monsters, monster-gear, affixes, opts)` резолвит гир
  (фолбэк «кулаки») → деривит → чемпион(реген мин.1) → аффиксы. `monstersSchema` → авторская форма
  (атрибуты+гир+role/tier/subfaction). 13 андедов в `monsters.json` (skeleton…skeleton-lord), крипт-пул биома.
  Все вызовы generateMonster обновлены. (коммит `93b1591`)
- ✅ **Ф5 частично:** выпадашки гира (weapon/armor/offhand из monster-gear) в редакторе. (коммит `6eb3cc8`)
- ⏳ **Осталось:** гир-афиксы (катать афиксы item-движком на монстро-гире; депрекейт monster-affixes);
  конфиг `subfactions` + тема аффиксов по подфракции; live-превью деривнутых статов; балансная калибровка.

### Открытый балансный сигнал
- Ростер стал заметно «мягче» старого (scout 14 hp vs старый скелет 15→75 по глубине; рост hp по уровню ~1.85×
  за 8 ур). Сильные типы (fanatic 17–32 урона на L1, berserker 13–26) сидят на роли `warrior` вместе с мягкими
  → мелкий «small»-рум может выкатить фанатика. **Решается тирами глубины (§8.A).**

- ✅ **Андед → зомби:** ростер `zombie-*` (13, Зомби-скаут…Зомби-главарь), спрайты mob-zombie/mob-archer,
  квесты/пулы биомов переведены. (коммит `01450bc`)

## 8. Следующая фаза (тиры глубины + генератор мобов + гир-афиксы)
### A. Тиры глубины (6 тиров) — контроль спавна по глубине сверх скейла
Новый конфиг `depth-tiers.json` (6 записей): `{ id, name, fromFloor, toFloor, weights }`, где
`weights` — вес спавна по СИЛОВОМУ тиру монстра (`weak/medium/strong/boss`, поле `tier` уже есть).
- Пример tier1 (этажи 1–3): `weak 90, medium 8, strong 2, boss 0` → «первые 3 этажа почти только
  скауты/рейдеры/метатели», охранник/боец/лучник изредка, фанатик/рыцарь/главарь — почти никогда.
- `spawnPacksEl`: по этажу → тир глубины → выбор монстра в пуле взвешен по его `tier`×вес (роль-состав
  пачки из `packs.json` сохраняется поверх). Дефолт-бэнды: 1:1-3, 2:4-6, 3:7-10, 4:11-15, 5:16-22, 6:23+.
- Редактор: вкладка «Тиры глубины» (авто-рендер + веса).

### B. Генератор мобов в редакторе (зеркало «Генератора предметов» `itemGen.ts`)
Новая вкладка `monsterGen.ts`: контролы Монстр/Уровень/Редкость(''/normal/magic/rare/champion)/Подфракция/
Кол-во. Одиночный ролл → карточка деривнутого стат-блока (hp, урон+тип, меткость/уворот/армор, резисты,
крит, скор.атаки, AI, гир, аффиксы) с цветом редкости; batch N → распределение редкостей/тиров + частота
аффиксов. Считает `generateMonster` на правленом конфиге.

### C. Редкость + гир-афиксы (унификация с item-движком)
Лестница normal/magic(1–2 аф.)/rare(3–4 + имя); `champion` — ортогональный элит-флаг (жирный hp/реген).
Генератор «одевает» монстра: катает афиксы item-движком (`affixes.json`, грань weapon/armor) на его гире →
модификаторы применяются к статам; подфракция смещает тему (`tagWeights`). `monster-affixes` депрекейт.
Ёмкая часть — маппинг item-стат → monster-стат.

### D. Подфракции
`subfactions.json`: `{ id, faction, name, tint, affixTheme[] }`, сид андедов (базовый/огонь/стужа/тлен/гроза),
выпадашка `monster.subfaction`. Consumed: C (тема аффиксов) + рендер (tint).

**Порядок:** A → B(normal) → D → C(+magic/rare, генератор дополняется). A и B дают видимый результат быстро.

### Прогресс §8
- ✅ **A1 движок** (`ffd90d2`): `depth-tiers.json` (6 тиров-бэндов, вес по силовому тиру), `monsters[].spawnCurve`
  (гибрид авто/оверрайд), `formulas/spawnWeight.ts` (+12 тестов), взвешивание в `spawnPacksEl`. Boss/strong
  отсечены рано, растут вглубь.
- ✅ **Пачки + XP** (`d333093`): `packs.json` пересобран под ростер (scout/warrior/thrower рано, роль warrior
  спанит все тиры → авто-скейл); XP деривации откалиброван `(12+level*5)×tierMult`. Замер: эт.1 weak 61% / мили 79–82%.
- ✅ **A2 редактор-график** (`d6a7619`): кастом-поле `spawnCurve` = редактор кривой глубины (form.ts:
  `fieldCustomRenderers` + проброс родителя). Гибрид авто/ручная, тянешь точки.
- ✅ **B генератор мобов** (`b1d043e`): вкладка «Генератор мобов» — ролл стат-блока по уровню + чемпион,
  пакет-распределение. Экспортированы monsterDerive/spawnWeight.
- ⏳ **Осталось:** до строгих 90% weak рано + weak-хвост глубоко лечит **depth-aware пачки** (какая пачка/веса
  на какой глубине) — отложено до бо́льшего набора пачек (решение юзера, докрутим тестами). Далее: **D** подфракции,
  **C** гир-афиксы + magic/rare (генератор дополнится редкостью).
